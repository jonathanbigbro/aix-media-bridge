# 0.6.0 — macOS preview

- Added a reusable Blender reference blockout skill with local-coordinate attachment identity, contact checks and continuous-time animation guidance.
- Added a standard-library cadence diagnostic for FFmpeg PGM sequences; distinguishes repeated-frame evidence from intentional holds and subjective motion quality.
- Added an AIX canvas organization skill with exhaustive classification, pinned layout plans, stale-state rejection, idempotent recognition and reload verification.
- Extended the media skill with reference-role separation, input capability checks, terminal-failure lineage, frame-indexed editing, visible-logo sizing, independent foley and asset-specific license records.
- Generalized all new guidance and synthetic tests. No private project media, prompts, identifiers, layout snapshots or development Git history are included.
- The published generation CLI remains schema 2; development-only multi-reference generation adapters are not shipped.

## 0.5.0 — macOS preview: narration, captions and native voiced export

- Separate edge-tts narration retains originals, word timings and 48 kHz PCM; no AIX calls.
- Resolve schema 2 adds exact 720-frame presentations, mono narration, editable Text+ captions, recorded local revisions and AAC export.
- Actual export checks detect silence, clipping, truncation, timing shifts and left-only routing; completed resume rechecks audio.
- Bounded original-chat history reconciliation preserves an unsuccessful Agent request without replay, pins the separate history proof, and checks original task/reference/count identity.
- Read the actual native image card through the production Vue vnode owner and bounded getter; preserve Markdown line breaks during exact task matching.
- Accept HTTP 200 as either the number or exact string supplied by MCP. An explicit locked migration rechecks every original parameter and retains the misclassified ledger; actual parameter failures remain blocked.
- Extract word-timed SRT and frame cues into a separate local caption module. Add reusable Skill references for creative intake, audio routing, Fusion commit, revision, playback and private-project delivery.
- One real AIX job completed with each upload/preparation/generation/download operation executed once; completed resume added zero submissions. A separate 720-frame voiced Resolve project was decoded, played and reopened. These passed after recorded interventions, not on another computer or new account.
- The 30-second intro remains a private workflow sample; editorial satisfaction and technical validation are separate. The four previously reviewed case files are the only public case media. Sealed releases remain unchanged.

# 0.4.2 — macOS preview: concurrent ledger safety

- Make CLI, daemon and direct-operation status a shared local read-only snapshot. Report parameter failures and observed phase without binding browser targets, rewriting ledgers/results/manifests or creating invalidation backups.
- Protect persistent generation validation, media ledger updates, final manifests and result invalidation with task ownership plus an exclusive mutation transaction. Read the latest ledger after locking. Verify CLI-to-daemon owner delegation; remove the restored-verification exemption and avoid reacquiring the caller's ownership lock during RPCs.
- Retain mutation exclusion for delayed daemon operations after the CLI releases ownership; reject stale-owner writes and competing writers. Keep 0.4.1 parameter guards and all original 50 tests unchanged.
- Add 19 deterministic synthetic tests, including six controlled status/writer interleavings and a separate writer process. Nine initial cases reproduced failure against 0.4.1 before fixes. Full local suites: 58 Node + 11 Python tests.

No dependency upgrade, AIX generation/upload/download, Resolve render or GitHub publication. Frozen 0.4.0/0.4.1 distributions and reviewed case bytes are preserved. See [validation](VALIDATION.md) for same-machine installation and live-validation limits.

# 0.4.1 — macOS preview: recovery and confirmation guards

- Persist submitted-parameter failures and missing evidence as a blocking state. Recheck legacy acknowledgements and original request bytes across run/resume, asset verification, completed recovery and final manifests. Retain assets and evidence; invalidate misleading completed results with byte-identical private backups. Native duration drift is a separate report field.
- Validate the corrected native image card's workflow, model, task, reference, count and prompt. Store correction/current-state evidence separately, preserve the original Agent response and recheck immediately before confirming. Unrecognized native component state stops before generation.
- Require Node.js >=22.12.0 in package metadata, the fixed lockfile root, npm engine-strict, setup, doctor and daemon. Keep Chrome DevTools MCP 1.9.0 and SDK 1.30.0 unchanged. Doctor reports source and running bridge versions separately.
- Preserve the original 30 tests and add 20 synthetic regression tests. Four key regression tests failed against the old logic before repairs. No AIX generation/upload/download or Resolve render was performed for this preview.

The reviewed story media and 0.4.0 distribution are preserved. Fresh-machine/new-account and post-fix real production acceptance remain unverified; see [validation](VALIDATION.md).

# 0.4.0 — AIX to DaVinci Resolve

This local release adds the completed editing workflow and the reviewed AIX story case to 0.3.0.

- Import verified AIX images and videos into a new owned Resolve project; create a 24 fps timeline with five seconds per shot; save a local DRP and export native H.264 MP4.
- Preserve project, timeline and render identities across repeated commands. Unknown results stop for inspection of the original operation.
- Commit Fusion privacy effects with native undo transactions and verify their connections, geometry, color and final rendered pixels. Include contact sheets for visual review.
- Recover an upload whose native operation succeeded but whose response timed out, using the prepared node and original file hash without uploading again.
- Include the reviewed 15-second story, three metadata-stripped preview frames, three generation templates, a Resolve template and a Chinese end-to-end guide.
- Pin approved media hashes and review records. Refuse unreviewed video, changed media and overwriting an existing version. Independently verify ZIP members, CRCs, hashes and archive metadata.

Validated on the same macOS machine and authorized AIX session used during development, with Resolve Studio 19. The story required a development-time upload recovery and a rejected first render before the final fixes. Fresh installation and offline tests are separate from live-generation validation; no new paid generation is performed merely to package this release. See [validation](VALIDATION.md).

The frozen 0.3.0 ZIP and its independent Git history remain unchanged. No GitHub publication is performed by this release build.
